# Claude API（任意）。設定すると台本生成にClaude AIを使用します。
# 未設定でも高品質テンプレートで動作します。
ANTHROPIC_API_KEY=

# 使用モデル（任意）。未設定時は claude-sonnet-4-6。
ANTHROPIC_MODEL=claude-sonnet-4-6

# トレンド集計cronの保護用シークレット（任意・本番推奨）。
# 設定すると /api/trends/refresh の実行に Authorization: Bearer <値> が必要。
CRON_SECRET=
